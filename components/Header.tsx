import React from 'react';

type Props = {

}

const Header = ({}: Props) => {
    return (
        <div>
            <nav>
                <div>
                    <button className="connectWalletButton">
                        Connect your wallet
                    </button>
                </div>
            </nav>
        </div>
    );
};

export default Header;
// by Rokas with ❤️
